# References

`references/` is for research only. It can contain external source snapshots or documentation notes, but it is not application source.

Do not edit, import, bundle, execute, or blindly copy code from `references/`. Runtime code belongs in `src/` and dependencies should come from npm packages whenever they are available to the project.
